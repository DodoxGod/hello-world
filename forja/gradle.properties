org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Fabric Properties - https://fabricmc.net/develop
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

# Mod Properties
version=1.0.0
maven_group=dev.forja
archives_base_name=forja

# Dependencies
fabric_api_version=0.158.0+26.2
# GeckoLib is required: the Fallen Smith has its own model and animations.
geckolib_version=5.5.5
# JEI is optional: the plugin only loads when the player has it installed.
jei_version=30.32.0.221
# Jade: optional at runtime, the plugin only loads when Jade is there.
jade_version=26.2.11+fabric
