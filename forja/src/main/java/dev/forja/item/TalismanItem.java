package dev.forja.item;

import net.minecraft.world.item.Item;

/** The item every talisman is; which gem it holds lives in its forja:talisman component. */
public class TalismanItem extends Item {
	public TalismanItem(Item.Properties properties) {
		super(properties);
	}
}
